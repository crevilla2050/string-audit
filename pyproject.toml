[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "string-audit"
version = "0.1.0"
description = "Lightweight CLI tool to detect hardcoded user-facing strings"
authors = [{ name = "Carlos" }]
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[project.scripts]
string-audit = "string_audit.cli:main"
